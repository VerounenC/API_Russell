<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Ajouter un Catway</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light min-vh-100 d-flex flex-column">
    <header class="bg-primary text-white text-center py-4">
        <h1>Ajouter un nouveau Catway</h1>
    </header>

    <main class="container my-5" style="max-width: 600px;">
        <form action="/catways" method="post" class="border rounded p-4 shadow bg-white">
            <div class="mb-3">
                <label for="catwayNumber" class="form-label">Numéro du Catway</label>
                <input type="number" id="catwayNumber" name="catwayNumber" class="form-control" required>
            </div>

            <div class="mb-3">
                <label for="catwayType" class="form-label">Type de Cateway</label>
                <select name="catwayType" id="catwayType" class="form-select" required>
                    <option value="">-- Sélectionnez --</option>
                    <option value="short">Short</option>
                    <option value="long">Long</option>
                </select>
            </div>

            <div class="mb-3">
                <label for="catwayState" class="form-label">État du Catway</label>
                <textarea name="catwayState" id="catwayState" class="form-control" rows="3" required></textarea>
            </div>

            <div class="text-center">
                <button type="submit" class="btn btn-success">Ajouter</button>
                <a href="/catways" class="btn btn-secondary ms-2">Retour</a>
            </div>
        </form>
    </main>

    <footer class="bg-light text-center py-3 mt-auto">
        &copy; 2025 Port de Plaisance Russell
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>