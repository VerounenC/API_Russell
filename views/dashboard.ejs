<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tableau de bord</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light min-vh-100 d-flex flex-column">
  
    <header class="bg-primary text-white py-4">
        <div class="container">
            <h1>Bienvenue <%= user.username %></h1>
            <p>Email : <%= user.email %></p>
            <p>Date : <%= new Date().toLocaleDateString('fr-FR') %></p>
        </div>
    </header>

    <main class="flex-grow-1 container py-5">
        <nav class="mb-4">
            <ul class="nav nav-pills justify-content-center">
                <li class="nav-items"><a class="nav-link" href="/catways">Gérer les catways</a></li>
                <li class="nav-items"><a class="nav-link" href="/reservations">Gérer les réservations</a></li>
                <li class="nav-items"><a class="nav-link" href="/users">Gérer les utilisateurs</a></li>
                <li class="nav-items"><a class="nav-link" href="/docs">Documentation</a></li>
                <li class="nav-items"><a class="nav-link" href="/logout">Se déconnecter</a></li>
            </ul>
        </nav>

        <section class="mt-5">
  <h2 class="text-center">Réservations en cours</h2>

  <% if (reservations.length === 0) { %>
    <p class="text-center text-muted">Aucune réservation active pour le moment.</p>
  <% } else { %>
    <div class="table-responsive">
      <table class="table table-striped text-center mt-3">
        <thead class="table-dark">
          <tr>
            <th>Catway</th>
            <th>Bateau</th>
            <th>Client</th>
            <th>Début</th>
            <th>Fin</th>
          </tr>
        </thead>
        <tbody>
          <% reservations.forEach(r => { %>
            <tr>
              <td><%= r.catwayNumber %></td>
              <td><%= r.boatName %></td>
              <td><%= r.clientName %></td>
              <td><%= new Date(r.startDate).toLocaleDateString() %></td>
              <td><%= new Date(r.endDate).toLocaleDateString() %></td>
            </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  <% } %>
</section>
     </main>

      <footer class="bg-light text-center py-3 mt-auto">
        &copy; 2025 Port de Plaisance Russell
    </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>