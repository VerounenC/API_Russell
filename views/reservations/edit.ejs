<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Modifier la Réservation</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light min-vh-100 d-flex flex-column">
    
    <header class="bg-primary text-white text-center py-4">
        <h1>Modifier la Réservation</h1>
    </header>
    
    <main class="container my-5">
        <form action="/reservations/<%= reservation._id %>?_method=PUT" method="POST" class="mx-auto border rounded p-4 shadow bg-white" style="max-width: 600px;">
            <div class="mb-3">
                <label for="catwayNumber" class="form-label">Numéro de catway</label>
                <input type="number" class="form-control" id="catwayNumber" name="catwayNumber" value="<%= reservation.catwayNumber %>" required>
            </div>

            <div class="mb-3">
                <label for="clientName" class="form-label">Nom du client</label>
                <input type="text" class="form-control" id="clientName" name="clientName" value="<%= reservation.clientName %>" required>
            </div>

            <div class="mb-3">
                <label for="boatName" class="form-label">Nom du bateau</label>
                <input type="text" class="form-control" id="boatName" name="boatName" value="<%= reservation.boatName %>" required>
            </div>

            <div class="mb-3">
                <label for="startDate" class="form-label">Date de début</label>
                <input type="date" class="form-control" id="startDate" name="startDate" value="<%= reservation.startDate.toISOString().split('T')[0] %>" required>
            </div>

            <div class="mb-3">
                <label for="endDate" class="form-label">Date de fim</label>
                <input type="date" class="form-control" id="endDate" name="endDate" value="<%= reservation.endDate.toISOString().split('T')[0] %>" required>
            </div>

            <button type="submit" class="btn btn-success">Enregistrer</button>
            <a href="/reservations" class="btn btn-secondary ms-2">Annuler</a>
        </form>
    </main>

    <footer class="bg-light text-center py-3 mt-auto">
        &copy; 2025 Port de Plaisance Russell
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>